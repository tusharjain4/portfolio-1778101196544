'use client';

// This component is intentionally left empty as the 'contact'
// section from the template is fully integrated into the Footer component
// to match the template's structure. The `footer-email` button
// handles the primary call to action for contact.

import React from 'react';

const Contact: React.FC = () => {
  return null; // Render nothing, as contact is handled in Footer.tsx
};

export default Contact;